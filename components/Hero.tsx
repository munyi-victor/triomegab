import { HeroContainer } from "@/styles/App.styles";

const Hero = () => {
  return (
    <HeroContainer>
      <div>
        <h1>We mentor, inspire and build the youths!</h1>
      </div>
    </HeroContainer>
  );
}

export default Hero;